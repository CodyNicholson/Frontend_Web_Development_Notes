# If Statements

Evaluators:

<, >, <=, >=, ===, !=, ==

'===' does not do type conversion, so ("1" === 1) will be false

'==' does have type conversion, so ("1" == 1) would be true

-

If Statement Syntax:

```javascript
if (tired === true)
{
    drinkCoffee();
}
else
{
    drinkWater();
}
```
